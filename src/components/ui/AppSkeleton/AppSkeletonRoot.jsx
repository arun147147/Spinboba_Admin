import { styled } from "@mui/material/styles";
import Skeleton from "@mui/material/Skeleton";

const AppSkeletonRoot = styled(Skeleton)(() => ({
  borderRadius: 8,
}));

export default AppSkeletonRoot;
